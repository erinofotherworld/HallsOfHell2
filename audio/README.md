Place your audio files here.

- Put `TheRestlessShadows.mp3` in this folder so the embedded player will find it at `audio/TheRestlessShadows.mp3`.

Note: browsers may block autoplay unless a user gesture starts playback; the player provides play/pause controls and will resume position across pages within the same tab (sessionStorage).